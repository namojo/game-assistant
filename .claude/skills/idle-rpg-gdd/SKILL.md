---
name: idle-rpg-gdd
description: "방치형(키우기) RPG 게임 디자인 문서(GDD)와 밸런스 모델 작성 스킬. 컨셉·전작 계승/개선 분류, 코어 루프·메타 루프(프레스티지·수집·시즌), 성장 곡선 수식(스테이지 HP/DPS/골드/강화 비용), 목표 진행 벽(D1/D3/D7/D30), 데이터 주도 콘텐츠 시스템(테이블 1행+에셋 1종), 수익모델(비확률 중심·확률형 표기 검증), 라이브 운영 훅, 분석 이벤트 스키마를 산출. '방치형 RPG 기획', '키우기 게임 GDD', '신작 기획안', '성장 곡선 설계', '밸런스 수식', '원시인 형님 2 기획' 요청이나 코딩 에이전트에 넘길 게임 설계 문서가 필요하면 이 스킬을 사용할 것. 구현 태스크 분해·AGENTS.md 작성은 codex-build-spec가 담당."
---

# Idle RPG GDD

방치형 RPG의 GDD와 밸런스 모델을 **시뮬레이터에서 먼저 돌아가는 형태**로 작성한다.

## 왜 이 순서인가
방치형의 사업 공식은 `에셋 생산량 × 밸런스 정확도 × 업데이트 주기`다. GDD가 화면·연출부터 시작하면 수치 시뮬레이션이 불가능한 시스템이 생기고, 그 시스템은 라이브에서 200스테이지 뒤의 벽으로 돌아온다. 그래서 이 스킬은 수식과 데이터 스키마를 먼저 고정하고 연출은 마지막에 붙인다.

## 절차

### 1. 컨셉과 전작 분류
전작 시스템을 **계승 / 개선 / 폐기 / 신규** 4열로 분류하고 이유를 한 줄씩 쓴다. 전작 정보 중 확인되지 않은 상세는 `(가정)` 표시. 컨셉 문장은 "누가(플레이어 판타지) 무엇을(핵심 동사) 왜 계속하는가(락인 장치)" 세 요소를 포함한다.

### 2. 루프 정의
- 코어 루프(초 단위): 자동 전투 → 골드 → 강화 → 스테이지 진행 → 벽 → 오프라인 보상/강화로 돌파
- 메타 루프(일·주 단위): 프레스티지(리셋 + 영구 보너스), 수집(도감·카드·스킨), 주간 콘텐츠(보스·탑·랭킹), 시즌
- 각 루프에 "벽을 만나는 시점"과 "벽을 넘게 하는 자원"을 명시한다. 벽 없는 방치형은 결제 동기가 없다.

### 3. 성장 곡선 수식
`references/balance-math.md`의 표준 수식군(스테이지 HP 지수 성장, DPS 다항+지수 혼합, 골드 드랍, 강화 비용 등비, 오프라인 보상 상한, 프레스티지 통화 산출)을 채택하고 파라미터 초기값을 정한다. 반드시 **목표 진행 벽 표**(D1/D3/D7/D30에서 무과금·소과금 유저가 도달하는 스테이지와 벽 체감 시각)를 먼저 쓰고 수식이 표를 만족하는지 검산한다. 시뮬레이터가 이 표를 회귀 테스트로 쓴다.

### 4. 데이터 주도 콘텐츠 스키마
모든 콘텐츠(형님·몬스터·장비·스킨·탈것·토템·스테이지·이벤트)는 JSON 테이블 1행으로 정의되고, 에셋은 id 규칙으로 참조된다. 스키마마다 필드·타입·범위·예시 1행을 적는다. 콘텐츠 1종 추가 비용이 "테이블 1행 + 에셋 1종"이 아니면 설계를 다시 한다.

### 5. 아트 파이프라인 친화 설계
에셋을 **원본(사람)** 과 **변형(AI 우선)** 으로 나눈다. 몬스터 등급별 팔레트, 장비 등급 색상, 스킨 의상 교체는 변형으로 설계해 원화 1장에서 N종이 나오도록 한다. 이것이 아트 AX 거버넌스(핵심 캐릭터는 사람)를 콘텐츠 구조에 박아 넣는 방법이다.

### 6. 수익모델과 규제
비확률(스킨·시즌 패스·광고 제거·성장 패키지) 중심. 확률형(뽑기·합성·슬롯)을 넣는다면: 확률 표기 위치, 표기값과 구현값의 자동 검증 방식(시뮬레이터 N회 시행 통계 비교), 천장(pity) 규칙을 GDD에 명시한다. 광고는 보상형 광고 슬롯을 시스템으로 설계한다.

### 7. 라이브 운영 훅과 분석 이벤트
이벤트 타입(기간 보스, 수집 이벤트, 성장 부스트), 시즌 구조, 랭킹. 분석 이벤트 스키마(session_start, stage_clear, upgrade, prestige, purchase, ad_reward, wall_hit)와 각 이벤트의 필수 필드.

### 8. 연출·UI는 마지막
화면 구성·연출은 시스템이 확정된 뒤 1절로 쓴다. 프로토타입 범위(MVP)와 확장 범위를 명시한다.

## 출력: GDD 템플릿
`references/gdd-template.md`의 목차를 따른다. 밸런스 모델은 별도 파일(`04b_balance_model.md`)로 분리해 시뮬레이터 구현자가 수식만 읽을 수 있게 한다.

## 참조
- `references/balance-math.md` — 방치형 표준 수식군, 파라미터 초기값 범위, 목표 진행 벽 표 형식, 검산 절차
- `references/gdd-template.md` — GDD 목차와 각 절의 최소 요건
