# 08. 최종 수정 내역 — 리더 결정 7건 반영

- **작업**: ax-deliverable-finalizer (리더 위임 최종 수정 담당) · 2026-09-18
- **근거 문서**: `07_review_report.md` 7.1 「사용자 결정이 필요한 항목 7건」 + 3절 I-6·I-7 + 6절 ⓐ + 7.2 L-2
- **원칙**: 각 문서의 `## 팀원 전달 메모` 절은 삭제하지 않았다. 처리된 메모 항목에는 `→ 리더 결정 …(08 참조)` 주석만 덧붙였다.
- **집계**: 수정 파일 **9개** · 변경 **59곳** · 신규 파일 1개(본 문서)

| 파일 | 변경 건수 | 해당 결정 |
|---|---|---|
| `03_strategy_ax_blueprint.md` | 2 | 결정 4, I-7 |
| `04_gdd_primitive_brothers_2.md` | 8 | 결정 4 |
| `04b_balance_model.md` | 2 | 결정 4 (+ 검수 B-1·B-2 잔여 수치 정정) |
| `05_codex_build_plan.md` | 27 | 결정 4·5·6·7, I-7, 잔여 리스크 L-2 |
| `05b_AGENTS.md` | 5 | 결정 4·6, I-7 |
| `06_proposal_main.md` | 7 | 결정 1·2·3, I-7, 첫 미팅 승격 |
| `06c_first_meeting_kit.md` | 4 | 첫 미팅 승격 |
| `07_review_report.md` | 8 | 7.1 각 항목 + I-7에 결정 줄 추가 |

---

## 결정 4 (I-6) — 확률 단위를 정수 ppm으로 통일

> 근거: 03 4.3.1의 "법적 방어 증거" 논리, 06 부록 E에 고객에게 이미 약속된 용어. 로드 시 `p = ppm / 1_000_000` 변환.

### `04_gdd_primitive_brothers_2.md`

| # | 절 | 변경 전 → 변경 후 |
|---|---|---|
| G4-1 | 8.0 공통 규칙 | `수치 단위: … 배수는 number, **확률은 number 0~1**. 시간은 초` → 확률 항목을 분리해 **4개 불릿 신설**: ① 확률·비율은 **정수 ppm**, 필드명 `*_ppm`, 값 0~1,000,000 정수(0.5% = `5000`) ② **변환 규칙** `p = ppm / 1_000_000`을 로더가 수행, 파일에 부동소수 확률을 쓰지 않음(수식 설명문의 소수 `p`는 ppm 병기 조건으로 허용) ③ 스키마 제약 `type: integer` + `minimum: 0` + `maximum: 1000000`, 시행별 **정수 합 = 1,000,000** ④ 근거(03 4.3.1 / 06 부록 E / 05 3.1) |
| G4-2 | 7.3 「뼈 갈기」 표기 확률 행 | `전설 0.5% / 영웅 3% / 희귀 15% / 일반 81.5%` → 각 값에 **ppm 병기**(`5,000` / `30,000` / `150,000` / `815,000 ppm`) + "테이블 저장값은 정수 ppm, 합 = 1,000,000" |
| G4-3 | 7.3 검증 방식 머리 문단 | `표기값 p에 대해 N=100,000 시행 시…` → `테이블 표기값 ppm을 p = ppm / 1_000_000으로 변환한 뒤 … **판정 입력은 언제나 정수 ppm이고 소수 p는 계산 중간값**` |
| G4-4 | 7.3 검증 방식 표 | 열 `표기 p` 1개 → **`표기값(ppm)` + `표기 p (= ppm/1e6)` 2개 열**로 분리. 행 값 `0.005 (0.5%)` → `**5,000** (0.5%) / 0.005` (영웅·희귀·일반 동일). σ·허용 구간·상대오차·N 값은 검수 정정본 그대로 유지 |
| G4-5 | 7.3 「주의(코딩 에이전트 필독)」 | `p = 0.005에서 N=100,000은…` → `**5,000 ppm**(p = 0.005)에서 N=100,000은…` |
| G4-6 | 7.3 테스트 규칙 불릿 | `표기 테이블(**balance.drop_rates**)을 직접 읽어` → `표기 테이블(**balance.drop_rates_ppm**)의 **정수 ppm 값**을 직접 읽어` + "4개 값의 정수 합이 1,000,000임을 같은 테스트에서 단언" 추가 |
| G4-7 | 8.4 `equipment` 스키마 + 예시 행 | `\| drop_weight \| number \| 0~1 \| 스테이지 드랍 가중치 \|` → `\| **drop_weight_ppm** \| int \| **0~1,000,000** \| …(ppm 정수 — 8.0). 0.35 → 350000 \|` · 예시 행 `"drop_weight":0.35` → `"drop_weight_ppm":350000` |
| G4-8 | 8.13 `balance` 스키마 + 예시 JSON | `\| drop_rates \| object \| 7.3 표기표 \|` → `\| **drop_rates_ppm** \| object \| 7.3 표기표(정수 ppm) \| … 전부 0~1,000,000 정수, **시행별 합 = 1,000,000** \|` · 예시 JSON `"drop_rates":{"bone_grind":{"legend":0.005,"hero":0.03,"rare":0.15,"common":0.815}}` → `"drop_rates_ppm":{"bone_grind":{"legend":5000,"hero":30000,"rare":150000,"common":815000}}` |

### `04b_balance_model.md`

| # | 절 | 변경 전 → 변경 후 |
|---|---|---|
| B4-1 | 8.4 확률 시스템 검증 기준 | ① **단위 문단 1줄 신설**: "확률은 데이터 테이블에서 **정수 ppm**으로 관리한다 … 아래 표의 소수 `p`는 `p = ppm / 1_000_000`로 얻은 계산 중간값" + 4개 값의 ppm 병기(합 1,000,000) ② 표에 `표기값(ppm)` 열 추가 ③ **부수 정정(검수 B-1·B-2 잔여)**: 0.005 행의 `±3σ 0.000212` → **0.000669**, 허용 구간 `[0.000288, 0.000712]` → **[0.004331, 0.005669]**, 상대오차 `42.4%` → **13.4%**, 시행수 산정식 표 0.5% 칸 `1,791,000` → **179,100**. 정정 근거를 인용 문단으로 병기 |
| B4-2 | 10절 전체 파라미터 요약 | `\| drop_rates.bone_grind \| {legend 0.005, hero 0.03, rare 0.15, common 0.815} \|` → `\| **drop_rates_ppm**.bone_grind \| {legend 5000, hero 30000, rare 150000, common 815000} (정수 ppm, 합 1,000,000) \|` |
| B4-3 | 9.4 회귀 단언 T5 | `표기값을 balance.drop_rates에서 읽어서` → `표기값(정수 ppm)을 **balance.drop_rates_ppm**에서 읽어서 … 정수 합 1,000,000 단언` |

### `05_codex_build_plan.md`

| # | 절 | 변경 전 → 변경 후 |
|---|---|---|
| P4-1 | 3.1 공통 규칙 | `확률은 number 0~1, 시간은 초, 배수는 number.` → `**확률·비율은 정수 ppm.** 필드명 *_ppm, 0~1,000,000 정수, 스키마 type: integer + minimum + maximum, 로드 시 p = ppm / 1_000_000, 시행 단위 정수 합 = 1,000,000` |
| P4-2 | 1.6 테이블 로딩 | **3번 항목 신설** — `*_ppm` 전 필드의 `Number.isInteger(v) && 0 <= v <= 1_000_000` 검사 + 시행별 정수 합 1,000,000 + **위반 시 기동 실패** + `tools/check-deps.ts`가 소스의 부동소수 확률 리터럴을 exit 1로 차단. 기존 3~6번은 4~7번으로 재번호 |
| P4-3 | 2절 npm 스크립트 표 | `check:deps` 설명에 **확률 리터럴 검사**(부동소수 확률 금지, `*_ppm` 정수 0~1,000,000 규칙) 추가 |
| P4-4 | 3.3 `equipment` 행 | 필수 필드 `drop_weight` → `drop_weight_ppm`, 제약 `drop_weight 0~1` → **`drop_weight_ppm` 정수 0~1,000,000**(0.35 → `350000`) |
| P4-5 | 3.4 `balance.json` | `"drop_rates": { "bone_grind": { "legend": 0.005, … } }` → `"drop_rates_ppm": { "bone_grind": { "legend": 5000, "hero": 30000, "rare": 150000, "common": 815000 } }` |
| P4-6 | 3.4 스키마 강제 제약 문단 | `drop_rates.bone_grind의 4개 값 **합이 1.0 ± 1e-9**` → `drop_rates_ppm.bone_grind의 4개 값은 **전부 0~1,000,000 정수**이고 **합이 정확히 1,000,000**(부동소수 허용 오차가 없어진다)` |
| P4-7 | T0.3 규칙 | 말미에 추가 — "확률 필드는 전부 `*_ppm` 정수(0~1,000,000), 스키마에 `type: integer`+`minimum`+`maximum`을 박는다" |
| P4-8 | T0.3 수용 기준 2 | 실패 케이스 ⑥ `drop_rates 합 ≠ 1` → `drop_rates_ppm.bone_grind의 **정수 합 ≠ 1,000,000**` · **케이스 ⑨ 신설**(확률 단위: 소수값 → `type: integer` 오류 / `1000001` → `maximum` 오류 / `drop_weight_ppm = 0.35` → 정수 오류) |
| P4-9 | T0.3 수용 기준 | **기준 5 신설** — `check:deps`가 `const p = 0.005;` 삽입 시 exit 1(`float probability literal`) · **기준 6 신설** — `grep -rn "drop_rates\b"` → 0줄(구 키 잔존 금지) |
| P4-10 | T1.6 규칙 | 드랍 판정 불릿 말미에 추가 — "드랍 가중치는 `equipment[].drop_weight_ppm`(정수)을 읽어 `w = ppm / 1_000_000`으로 변환해 쓴다. 코드에 소수 가중치를 쓰지 않는다" |
| P4-11 | T2.6 목표 | `balance.drop_rates에서 읽어` → `**balance.drop_rates_ppm**(정수 ppm)에서 읽어 `p = ppm / 1_000_000`으로 변환한 뒤` |
| P4-12 | T2.6 규칙 | 시행수 불릿에 등급별 ppm 병기(`전설(5,000 ppm = 0.005)` …) + **단위 불릿 신설**(입출력·리포트 표기값은 정수 ppm, 정수 합 1,000,000 단언) |
| P4-13 | T2.6 수용 기준 1·2 | 출력 열 `grade, declared p, N, …` → `grade, **declared_ppm**, p, N, **observed_ppm**, …` · 기준 2에 `declared_ppm` 열 `5000 / 30000 / 150000 / 815000`(합 1,000,000) 단언 추가 |
| P4-14 | T2.6 수용 기준 4 | grep 금지 리터럴에 **ppm 정수(`5000`·`30000`·`150000`·`815000`)를 추가** — 부동소수·ppm 양쪽 하드코딩 금지 |
| P4-15 | 6.5 시행수 표 | 표에 `표기값(ppm)` 열 추가(5,000 / 30,000 / 150,000 / 815,000) + "입력은 `drop_rates_ppm`의 정수 ppm, `p_i = ppm_i / 1_000_000`" 문장 + 합 1,000,000 각주 |
| P4-16 | 6.4 회귀 단언 T5 | `확률 표기값을 balance.drop_rates에서 읽어 검증` → `확률 표기값(정수 ppm)을 **balance.drop_rates_ppm**에서 읽어 검증 + 정수 합 1,000,000 단언` |
| P4-17 | T6.4 규칙 | `표기값은 balance.drop_rates에서 읽어 렌더링` → `**balance.drop_rates_ppm**(정수 ppm)에서 읽어 `p = ppm / 1_000_000` 변환 후 퍼센트로 렌더링` |
| P4-18 | T6.4 수용 기준 5 | grep 금지 패턴에 ppm 정수(`5000`·`815000`) 추가 |
| P4-19 | 8.2 모호 지점 9 | `표기표에서 자동 산출. 전설 0.5% → N=1,800,000` → `표기표(**drop_rates_ppm**, 정수 ppm)에서 자동 산출. 전설 **5,000 ppm** → N=1,800,000 **유지**(리더 결정 ⑦) + 60초 초과 시 CI 프로파일 한정 폴백` |
| P4-20 | 8.3 A-D14 | `확률값을 **부동소수 0~1**로 관리한다 … 본 플랜은 그 규격을 따르지 않았다 / 상태 **가정(이탈 기록)**` → `확률값을 **정수 ppm**으로 관리한다(`drop_rates_ppm`, `drop_weight_ppm`, 0~1,000,000 정수, 합 1,000,000, `p = ppm / 1_000_000`) / 영향 열에 반영 위치 전량 / 상태 **결정 완료: ppm 채택**(리더 결정 ④ · 08 참조)` |

### `05b_AGENTS.md`

| # | 절 | 변경 전 → 변경 후 |
|---|---|---|
| A4-1 | 규칙 7 (데이터) | `반드시 **balance.drop_rates**에서 읽는다` → `반드시 **balance.drop_rates_ppm**에서 읽는다` |
| A4-2 | 규칙 7-1 **신설** | "확률·비율은 정수 ppm. 필드명 `*_ppm`, 0~1,000,000 정수, 로드 시 `p = ppm / 1_000_000`, 시행 단위 정수 합 = 1,000,000. **부동소수 확률 리터럴 금지 — `check:deps`가 exit 1로 막는다.** 소수 `p`는 계산 중간값으로만 존재한다" |

### `03_strategy_ax_blueprint.md`

| # | 절 | 변경 전 → 변경 후 |
|---|---|---|
| S4-1 | 4.3.1 「설계 결정」 불릿 | 말미에 추가 → `**→ 리더 결정(2026-09-18): 이 규격을 전작 시뮬레이터뿐 아니라 신작 GDD·빌드 플랜에도 동일 적용하기로 확정. 반영 위치는 GDD 8.0·7.3·8.4·8.13, 빌드 플랜 3.1·3.3·3.4·1.6·T0.3·T2.6, 밸런스 모델 8.4·10절(08 참조).**` |

---

## 결정 (I-7) — 시뮬레이터 CLI·테이블 규격을 03 메모 ②에 맞춤

### `05_codex_build_plan.md`

| # | 절 | 변경 전 → 변경 후 |
|---|---|---|
| P7-1 | 0절 설계 전제 | `**총 태스크 40개** (P0:4 / P1:10 / **P2:8** / …)` → `**총 태스크 41개** (… / **P2:9** / …) + "P2가 8→9로 늘어난 것은 `sim replay` 재현 태스크 **T2.9**를 신설했기 때문"` |
| P7-2 | 3.1 공통 규칙 | **테이블 JSON 공통 메타 3필드 불릿 신설** — `schema_version`·`generated_at`·`checksum`(`sha256:<hex>`, 개행 정규화 후)을 전 파일 필수로. 배열 테이블은 `{schema_version, table, generated_at, source, checksum, rows}` 봉투, `balance.json`은 `row` 단일 객체. **3필드 누락 시 기동 실패**. 근거(03 메모 (2)-①(e), `sim replay` 재현 키) 병기 |
| P7-3 | 1.6 테이블 로딩 | **공통 메타 검증 항목 신설**(현 4번) — 3필드 존재 확인 + `checksum` 본문 재계산 대조 + `_manifest.json`과 교차 대조, 불일치 시 **기동 실패** |
| P7-4 | 2절 npm 스크립트 표 | `npm run sim:replay -- --run <run.json>` 행 **신설** — "기록된 실행을 시드 + 테이블 checksum으로 재현(규격 `sim replay` 별칭, T2.9). 재현 실패·checksum 불일치는 **exit 2**" |
| P7-5 | 4절 Phase 2 머리글 | `### Phase 2 — 헤드리스 시뮬레이터와 밸런스 회귀 (**8태스크**)` → `(**9태스크**)` |
| P7-6 | 4절 Phase 2 | **T2.9 태스크 카드 신설** — `sim replay` 시드+checksum 재현(규제 증거). 의존 T2.3·T2.4, 산출 `packages/sim/src/replay.ts` + `npm run sim:replay`, 규칙 4개(인자 전량 복원 / checksum 불일치 = **폴백 없이 exit 2** / stdout JSON / **T2.4와 같은 비교기 재사용**), 수용 기준 5개(전부 `실행 명령 → 기대 결과` 형식: 정상 재현 exit 0, 테이블 변경 exit 2, 시드 변조 exit 2, 테스트 5 passed, 확률 산출물 재현), 크기 S, 참고(T2.4와 범위 구분) |
| P7-7 | 5.2 대응표 | `13.3 성공 판정 4개` 행의 `2→T2.4` → `2→**T2.4, T2.9**(재현)` · **행 신설** `7.3 확률 검증 증거 3종(03 4.3.1) → ① 재현 로그 **T2.9** / ② 분포 검증 리포트 **T2.6** / ③ 버전별 테이블 diff **T2.7**` |
| P7-8 | 6.1 CLI 코드 블록 | `npm run sim:replay -- --run reports/…/summary.json # 기록 재현 (T2.9)` 행 추가 |
| P7-9 | 6.1 옵션 표 | `--run`(sim:replay 전용, 필수) / `--format`(json\|md\|json,md) / `--thresholds`(sim:diff 전용, 코드 상수 금지) **3행 신설** |
| P7-10 | 6.1.1 **신설** | 「규격 서브커맨드 ↔ npm 스크립트 별칭 (정본 대조표)」 표 — `sim run`↔`npm run sim`(T2.1) / `sim diff`↔`sim:diff`(T2.7) / `sim verify-odds`↔`sim:prob`(T2.6) / `sim replay`↔`sim:replay`(**T2.9 신설**) + 규격 외 CI 별칭 3종(`sim:regress`·`sim:determinism`·`sim:long`). **기존 npm 스크립트 이름은 유지**하고 이 표를 정본 대조표로 선언 |
| P7-11 | 6.1.2 **신설** | 「exit code 규약」 표 — `0` 정상 / `1` 실행 오류(인자·파일·스키마·공통 메타 누락) / `2` **임계 위반 → 실패 + 배포 차단**. 1과 2를 나누는 이유 1문단 |
| P7-12 | 6.1.3 **신설** | 「출력 규약」 — **stdout = 기계 판독 JSON 1개 객체**, **stderr = 사람용 요약**, `--format md`(+`json,md`)로 마크다운 리포트 |
| P7-13 | 6.4 회귀 단언 표 | `+4` 행 신설 — "기록(`run.json`)만으로 재현 — 시드 + 테이블 checksum 일치 / T2.9 / `npm run sim:replay -- --run <run.json>` / CI 필수(확률 리포트 생산 시)" |
| P7-14 | T0.3 규칙·수용 기준 | 규칙에 "전 데이터 파일에 공통 메타 3필드를 붙이고 로더가 checksum을 재계산해 대조" 추가 · 수용 기준 2에 **케이스 ⑩ 신설**(checksum 1자 변경 → 기동 실패, `schema_version` 삭제 → 필수 필드 오류) · 수용 기준 3 출력 문자열에 `schema_version 1.0, checksum OK (12/12 recomputed), … ppm OK` 반영 |
| P7-15 | T2.6 수용 기준 1 | 말미에 추가 — "임계 위반 시 **종료 코드 2**(6.1 exit code 규약)" |
| P7-16 | 8.3 A-D15 | `CLI를 npm run sim/... 체계로 둔다 … 규격과 다르다 / 상태 **가정(이탈 기록)**` → `**규격 서브커맨드를 정본 인터페이스로 삼고 npm 스크립트는 별칭으로 유지**(정본 대조표 6.1.1) + exit code 0/1/2 + stdout JSON/stderr/`--format md` + 테이블 공통 메타 필수 / 영향 열에 반영 위치 전량(T2.9 신설 포함) / 상태 **결정 완료**(리더 결정 I-7 · 08 참조)` |

### `05b_AGENTS.md`

| # | 절 | 변경 전 → 변경 후 |
|---|---|---|
| A7-1 | 「명령」 코드 블록 | 각 `sim:*` 행 주석에 규격 별칭 병기(`[= sim run]`·`[= sim verify-odds]`·`[= sim diff]`) + **`npm run sim:replay -- --run <run.json>` 행 신설**(`[= sim replay]`) |
| A7-2 | 「명령」 절 말미 **신설** | ① **서브커맨드 별칭** 안내(정본 대조표는 `docs/BUILD_PLAN.md` 6.1.1, **npm 스크립트 이름을 바꾸지 않는다**) ② **exit code 규약 표**(0/1/2 + 코드 2일 때의 에이전트 행동 = `balance.json`을 건드리지 않고 보고) ③ **출력 규약**(stdout JSON / stderr 요약 / `--format md`) |
| A7-3 | 규칙 7-2 **신설** | "모든 데이터 파일에 공통 메타 3필드(`schema_version`·`generated_at`·`checksum`)가 있다. 필드를 지우거나 checksum을 손으로 고치지 않는다. 테이블을 바꿨으면 `gen-manifest.ts`로 재생성한다. 이 셋은 확률 검증 리포트와 `sim:replay`의 재현 앵커다" |

### `06_proposal_main.md` · `03_strategy_ax_blueprint.md`

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| R7-1 | `06` 8.6 별첨 안내 표 | `**태스크 40개**(P0 4 / P1 10 / **P2 8** / …)` → `**태스크 41개**(… / **P2 9** / …)` |
| R7-2 | `06` 12절 다음 단계 표 2행 | `**8개 카테고리** 체크리스트` → `**9개 카테고리** 체크리스트(A~H 자료 + **I. 신작 검토용 확인 질문 14건**)` |
| S7-1 | `03` 팀원 전달 메모 (2) 말미 | 「분기 대비」 문단 앞에 **인용 블록 신설** — ①의 ppm·공통 메타는 빌드 플랜 3.1·1.6에, ②의 서브커맨드·exit code·stdout JSON은 6.1.1~6.1.3에 반영됐고 `sim replay`는 **T2.9**로 신설, A-D14·A-D15는 **결정 완료**로 갱신됐음을 기록(08 참조) |

---

## 결정 1 — CS 착수 프레이밍 현행 유지 + 방어 문장 1칸 추가

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| D1-1 | `06` 7.2 우선순위 표 · **1-a 행 「착수 게이트」 칸** | `티켓·로그 읽기 권한만. 사내 코드 접근 불필요` → `… 사내 코드 접근 불필요. **1-b·1-c의 착수 게이트가 열리기를 기다리는 2~3주를 메우는 용도이며, 아트·QA보다 앞세우는 것이 아닙니다**(7.3절)` |

> 7.3절 본문(종속 프레이밍)은 **그대로 유지**했다. 이제 표와 본문 두 곳에서 같은 논리가 나오므로 구두 되물음에 두 번 답할 수 있다.

---

## 결정 2 — H2(전작 유저 무료 이관) 가설 노출

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| D2-1 | `06` 8절 말미 | **8.7 「이 트랙이 서 있는 가장 취약한 전제」 신설**(8.6 별첨 안내 뒤) → ① "이 트랙의 유입 논리는 **H2(전작 유저 무료 이관)** 하나 위에 서 있다 … **착수 여부는 전작 유저의 자체 유입이 성립하는지를 먼저 검증한 뒤 결정합니다**" ② "**H2가 기각되면 신작 트랙의 범위를 재설정합니다** — 프로토타입은 진행 가능하되 투입 규모·출시 목표·성공 기준을 다시 잡아야 하며, 그 판단은 3년 무게 중심 결정(12절)과 함께 한다. 저희가 대신 정하지 않습니다" |

---

## 결정 3 — T+12개월 유지 + 유보 문장을 11.3 말미로 집결

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| D3-1 | `06` 11.2 말미 인용 문단 | `… 여력이 부족하다고 판단되면 **11.3절의 종료 목표 시점을 조정해 다시 제시하겠습니다.** 지금 낙관한 시점은 나중에 "결국 못 나가더라"로 돌아옵니다.` → `… 첫 미팅에서 실제 인원과 직무 배분을 확인하겠습니다. **그 결과가 종료 목표 시점에 어떻게 반영되는지는 11.3절 말미에 함께 적었습니다.**` (조정 규칙 본문을 11.3으로 이관) |
| D3-2 | `06` 11.3 말미 | **문단 신설** — "**목표 시점의 조정 규칙**: … 여력이 부족하면 T+12개월을 조정해 다시 제시하겠습니다. 지금 낙관한 시점은 나중에 '결국 못 나가더라'로 돌아옵니다. **다만 시점을 조정해도 위 종료 조건 3개는 바뀌지 않습니다.** 조정되는 것은 '언제'이고 '무엇을 만족해야 끝인가'는 고정입니다" → 11.3이 **목표 시점 → 조건 3개 → 조정 규칙** 한 흐름으로 읽힌다 |

---

## 결정 5 — `equip_recovery_mode` 이중 모드 한시 유지 + 종료 기한 명기

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| D5-1 | `05` 8.1 리더 결정 표 · **D3 행** | `**목표 벽 표 재검산이 필요하다**(GDD 메모 3-5) / 상태 **결정**(재검산은 기획 담당)` → 결정 내용 말미에 **기한 추가**: "**이중 모드는 한시적이다(리더 결정 ⑤) — 기한: T2.7 격차 리포트 수령 후 2주 내에 기획이 목표 벽 표를 `drop_wait` 기준으로 재검산하고, 그 시점에 회귀 기준(T2.5)을 `instant`→`drop_wait`로 이관한다. 이관과 동시에 `instant`는 diff 전용으로 강등하며, 기한 내 이관이 없으면 `instant` 모드와 `equip_recovery_mode` 키를 삭제하고 `drop_wait` 단일 모드로 간다.**" / 상태 → `**결정**(재검산은 기획 담당 · **이관 기한 T2.7+2주**)` |
| D5-2 | `05` 8.2 모호 지점 표 · **5행** | `… 격차 리포트로 회신 / **결정**(기획 재검산 대기)` → `… 격차 리포트로 회신. **이중 모드 종료 기한 = T2.7 리포트 + 2주**(리더 결정 ⑤) / **결정**(기획 재검산 대기 · 기한 명시)` |
| D5-3 | `05` 팀원 전달 메모 2)-② | (삭제하지 않고) 말미에 주석 추가 → `**→ 리더 결정 ⑤(08 참조): 이중 모드를 한시 유지하되 8.1 D3에 종료 기한을 명기했습니다.**` |

---

## 결정 6 — 에셋 origin 게이트의 로컬 우회 방지 (pre-commit)

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| D6-1 | `05` T0.4 산출 | 말미에 추가 → `**`simple-git-hooks` 기반 pre-commit 훅 설정**(`package.json`의 `simple-git-hooks.pre-commit` = `npm run validate:assets`)` — 훅 관리자는 **`simple-git-hooks`로 지정**(husky 미채택) |
| D6-2 | `05` T0.4 수용 기준 | **기준 5 신설** — `npx simple-git-hooks` 후 `cat .git/hooks/pre-commit`에 `npm run validate:assets` 포함, 위반 에셋(`reviewed_by:""`) 스테이징 후 `git commit` → **종료 코드 1**, 커밋 미생성 · **기준 6 신설** — `npm test -w tools -- hooks` → **2 passed**(`simple-git-hooks.pre-commit` 키 존재 / `postinstall` 등록으로 `npm ci` 후 자동 설치) |
| D6-3 | `05` T0.4 참고 | 말미에 추가 → 로컬 우회(`npm ci --ignore-scripts`) 차단이 5·6번의 목적이고 훅 관리자는 `simple-git-hooks`로 **지정**한 이유(의존성이 작고 `package.json` 한 블록). `--no-verify`로 뚫리는 경우는 CI 독립 잡이 막는다 — **로컬 게이트 = 실수 방지 / CI 잡 = 우회 방지**로 역할 분리 |
| D6-4 | `05b` 규칙 13 → 13-1 **신설** | 기존 규칙 13 괄호의 `(--ignore-scripts 금지)`를 떼어내 **독립 규칙 13-1**로 승격 → "`npm ci --ignore-scripts` / `npm install --ignore-scripts` / `git commit --no-verify` / 훅 삭제·주석 처리 **전부 금지**. 설치는 항상 `npm ci`, pre-commit 훅 설치 여부는 `cat .git/hooks/pre-commit`으로 확인. 이 게이트는 '핵심 캐릭터 원화는 사람' 거버넌스를 코드로 강제하는 유일한 장치이고 그 약속은 고객 문서에 이미 적혀 있다 — 로컬에서 뚫리는 게이트는 약속의 근거가 되지 못한다" |
| D6-5 | `05` 팀원 전달 메모 3) 2번째 불릿 | (삭제하지 않고) 말미에 주석 추가 → `**→ 리더 결정 ⑥(08 참조): T0.4에 pre-commit 수용 기준 2건을 추가하고 AGENTS.md에 규칙 13-1을 신설했습니다.**` |

---

## 결정 7 — 확률 검증 채택 N = 1,800,000 유지 + CI 폴백

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| D7-1 | `05` T2.6 수용 기준 5 | `전설 1.8e6 시행 실행 시간이 **60초 이내**` → `… **60초 이내**. **60초를 초과하면(리더 결정 ⑦) `PROB_N_PROFILE=ci`로 전설 N = 200,000 폴백을 허용한다** — 폴백값은 **산식 최소치 179,100 이상**이어야 하고(하네스가 `N >= ceil(900(1−p)/p)`를 단언), 리포트 머리글에 `profile: ci (N=200000, formula min 179100)`을 출력한다. 기본(증거 생산용) 프로파일은 그대로 1,800,000이며 폴백은 **CI 프로파일에서만** 쓴다` |
| D7-2 | `05` 6.5 표 아래 문단 | `… T2.6 수용 기준 5(60초 이내)를 못 지키면 **산식값 기준으로 내리는 것을 기획과 합의한다**` → `… **리더 결정 ⑦로 유지가 확정됐다.** 다만 60초를 못 지키는 경우에 한해 `PROB_N_PROFILE=ci`에서 N = 200,000 폴백을 허용한다(산식 최소 179,100 이상 단언). **증거 생산용 기본 프로파일은 1,800,000 그대로다**` |

---

## 첫 미팅 확인 항목 승격 (07 6절 ⓐ) + 잔여 리스크 등록

| # | 파일·절 | 변경 전 → 변경 후 |
|---|---|---|
| M-1 | `06c` 4절 | **「I. 신작 검토용 — 전작 시스템 확인(가능한 범위)」 카테고리 신설**(H 다음) — 머리말("자료가 아니라 **확인 질문 14건**, 문서를 만들지 마시고 구두로 답해 주셔도 됩니다 / '그건 다릅니다'가 가장 가치 큰 답") + **A1~A14 표**(가정 / 확인 질문 / 답이 다르면 흔들리는 것) + 체크박스 2줄(답할 수 있는 것만 / **A4·A10·A13은 세션 B에서 먼저**) |
| M-2 | `06c` 4절 **B. 사업·지표** | 말미에 1줄 추가 → `- [ ] **실제 세션 길이 분포** — 1회 접속당 플레이 시간과 일 접속 횟수 (가능하면 **분석 도구 export** 그대로, 없으면 체감으로도 충분합니다)` (07 ⓑ의 05 U-9 대응) |
| M-2b | `06c` 5절 사전 요청 메일 전문 | `요청 자료는 여덟 갈래입니다 … 상세 목록은 첨부 문서를 참고해 주십시오.` → 말미에 1문장 추가 → "여기에 한 갈래를 더 붙였습니다 — **신작 검토용 「전작 시스템 확인」 질문 14건**입니다. 자료가 아니라 질문이라 문서를 만드실 필요 없이 **구두로 답해 주셔도 충분합니다**" (I 카테고리 신설에 따른 메일 본문 정합) |
| M-3 | `06` 부록 B | 카테고리 표에 **`I. 신작 검토용 — 전작 시스템 확인` 행 추가** + 그 아래 **「B-1. 확인 질문 14건」 소절 신설**(06c와 같은 A1~A14 표 + 용도 문단: 답이 모이면 별첨 GDD를 고쳐 다시 제공, A4·A10·A13은 세션 B 우선) |
| M-4 | `05` 8.4 미결 표 | **U-10 신설** — "사전 계산 테이블의 누적 오차 — `s=1200` 근처에서 T1.1 수용 기준 2(상대오차 1e-3)를 못 지킬 경우의 보정 설계가 없다". 왜 미결인가(반복 곱의 오차가 `i`에 비례 누적 / 안전망이 T1.1 기준 2 하나뿐) + 해소 경로(**T1.1 첫 실행 시 `s=1000~1400` 상대오차 측정 → 1e-3 초과 시 ① 구간별 재정규화 설계를 별도 태스크로 신설 ② 해당 구간을 `[측정 불가]` 표시. 결정성(D1)은 되돌리지 않는다**) — 07 7.2 L-2 대응 |
| M-5 | `05` 8.4 U-9 | 해소 경로 말미에 추가 → `**첫 미팅 사전 요청 자료 B에 "실제 세션 길이 분포(가능하면 분석 도구 export)"로 등록됨**(06c 4절 B)` |
| M-6 | `05` 팀원 전달 메모 3)-③ | (삭제하지 않고) 주석 추가 → `**→ 08 반영: 8.4에 U-10으로 등록했습니다.**` |

> **표의 3번째 열 처리**: 07 6절 ⓐ 원표는 "흔들리는 것" 칸에 내부 식별자(`진단 U-08`, `03 4.2`, `04b 5절` 등)를 썼다. 06·06c는 **고객 제출본**이므로 내부 문서 식별자를 **고객이 읽을 수 있는 참조**로 바꿔 적었다(예: `진단 U-08` → "확률 표기-구현 정합 검증의 규모(기획서 7.2절 1-β)", `03 4.2` → "아트 파이프라인의 성립 여부(기획서 7.2절 1-b·8.5절)"). **14건의 가정·질문 내용과 순서는 원표 그대로**다.

---

## `07_review_report.md` — 결정 결과 주석 (8곳)

| # | 절 | 추가 내용 |
|---|---|---|
| V-1 | 7.1 ① | `→ 리더 결정: 현행 종속 프레이밍 유지 + 06 7.2 표 1-a 「착수 게이트」 칸에 방어 문장 추가 (08 참조)` |
| V-2 | 7.1 ② | `→ 리더 결정: 노출한다. 06 8.7 신설 — H2 가설 + "기각 시 신작 트랙 범위 재설정" (08 참조)` |
| V-3 | 7.1 ③ | `→ 리더 결정: T+12개월 유지. 11.2 유보 문장을 11.3 말미로 이동 + "조건 3개 불변" (08 참조)` |
| V-4 | 7.1 ④ | `→ 리더 결정: 정수 ppm 통일. 04·04b·05·05b 반영 위치 전량 열거, A-D14 결정 완료 (08 참조)` |
| V-5 | 7.1 ⑤ | `→ 리더 결정: 이중 모드 한시 유지 + 05 8.1 D3에 T2.7+2주 이관 기한 명기 (08 참조)` |
| V-6 | 7.1 ⑥ | `→ 리더 결정: pre-commit 훅 도입(`simple-git-hooks` 지정), T0.4 수용 기준 5·6 + AGENTS.md 규칙 13-1 (08 참조)` |
| V-7 | 7.1 ⑦ | `→ 리더 결정: N=1,800,000 유지 + 60초 초과 시 CI 프로파일 N=200,000 폴백(산식 최소 179,100 이상) (08 참조)` |
| V-8 | 3절 I-7 권고 말미 | `→ 리더 결정: 권고대로 처리 + 확장. 6.1.1/6.1.2/6.1.3 신설, T2.9 추가(40→41), 공통 메타 3필드, AGENTS.md 동일 갱신, A-D15 결정 완료 (08 참조)` |

---

## 검증 결과

| 검사 | 명령 | 결과 |
|---|---|---|
| 구 키 `drop_rates` 잔존 | `grep -rn "drop_rates\b" 0*.md \| grep -v drop_rates_ppm` | **산출물 0건.** `07`의 2줄만 남았고 이는 **수정 전 상태를 인용한 검수 기록**이므로 의도적으로 보존 |
| 구 키 `drop_weight` 잔존 | `grep -n "drop_weight[^_]" 04*.md 05*.md` | **0건** |
| 04·05 예시 행의 부동소수 확률 | `grep -n "0\.005\|0\.03\|0\.15\|0\.815\|0\.35"` | **예시 행·JSON에 0건.** 남은 것은 ① 7.3/6.5/8.4 **검증표의 `표기 p` 열**(ppm 병기된 계산 중간값 — 허용) ② `ember_c 0.35`·`Q = 0.35·s^1.45`(**확률이 아닌 프레스티지 계수**) ③ `collection.rewards.value 0.005`(**확률이 아닌 스탯 보너스 +0.5%**) ④ 하드코딩 금지 grep 패턴 문자열 자체 |
| 05 태스크 수 | `grep -c "^#### T[0-9]"` | **41개** (P0 4 / P1 10 / **P2 9** / P3 4 / P4 5 / P5 4 / P6 5) — 0절 표기·Phase 2 머리글·실측 3자 일치 |
| 05 5절 총수 ↔ 06 8.6 | `grep -n "태스크 4[01]개"` | **41개로 일치.** 05 0절 `총 태스크 41개` = 06 8.6 `태스크 41개(P0 4 / P1 10 / P2 9 / P3 4 / P4 5 / P5 4 / P6 5)` |
| 팀원 전달 메모 보존 | `grep -c "^## 팀원 전달 메모"` | 03·04·05·06 **각 1건 유지**(삭제 0건, 주석만 추가) |
